@using Collections.Web.ViewModels.Item
@model CreateItemViewModel

@{
    ViewData["Title"] = "Create item";
}

<div class="card">
    <div class="card-header">
        <h3 class="text-uppercase">New item</h3>
    </div>
    <div class="card-body">
        <form asp-action="Create" asp-controller="Item" asp-route-returnUrl="@Model.ReturnUrl">
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="form-group">
                        <label asp-for="Description" class="control-label"></label>
                        <textarea asp-for="Description" class="form-control"></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>
                </div>
            </div>
            <br>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-sm btn-primary rounded-0" />
            </div>
        </form>
    </div>
</div>